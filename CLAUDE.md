# CLAUDE.md — دليل العمل على المشروع ده

> **اقرأ الملف ده بالكامل قبل ما تكتب سطر كود واحد.**
> ده أول ملف أي Agent أو مطوّر لازم يفتحه.

---

## 1. إيه المشروع ده؟

موقع شخصي عربي لمدرّب ذكاء اصطناعي، بيعرض كورساته على **٣ مستويات**
(مبتدئ / متوسط / متقدّم)، بتصميم راقي، ومبني بحيث يتوسّع بعدين (دفع، APIs، منصة تعلّم).

---

## 2. اقرأ إيه قبل ما تشتغل (بالترتيب ده)

| # | الملف | ليه |
|---|-------|-----|
| ١ | `CLAUDE.md` (ده) | القواعد والممنوعات |
| ٢ | `docs/PLAN.md` | الخطة الكاملة: الهيكل، الأنواع، الصفحات، المكوّنات، مراحل التنفيذ |
| ٣ | `docs/DESIGN.md` | كل قرار بصري بالمقاس والرقم |
| ٤ | `docs/CONTENT.md` | كل النصوص ونبرة الكتابة |
| ٥ | `docs/ROADMAP.md` | خطة التوسّع بعدين |
| ٦ | `docs/SOURCE-MATERIAL.md` | المادة الأصلية — ممنوع تخترع محتوى مش فيها |
| ٧ | `docs/MEMBERSHIP-PLAN.md` | خطة الحسابات والوصول المدفوع (المرحلة الجاية) |

**قاعدة الأولوية عند التعارض:**
`DESIGN.md` (للبصريات) > `PLAN.md` (للبنية) > الكود الموجود.
يعني: **لو الكود مخالف للتوثيق، الكود هو اللي يتصلّح.**

---

## 3. الأوامر

```bash
npm install          # تثبيت الحزم
npm run dev          # تشغيل محلي على http://localhost:3000
npm run build        # بناء للإنتاج — لازم ينجح قبل أي commit
npm run typecheck    # فحص الأنواع — لازم ينجح قبل أي commit
npm start            # تشغيل نسخة الإنتاج
```

**قبل أي commit، الاتنين دول لازم يعدّوا:**
```bash
npm run typecheck && npm run build
```

---

## 4. الممنوعات المطلقة ⛔

| # | ممنوع | ليه | البديل |
|---|-------|-----|--------|
| ١ | كتابة نص ظاهر للمستخدم جوّه مكوّن | عشان نقدر نغيّر المحتوى من مكان واحد ونربطه بقاعدة بيانات | حطّه في `src/content/` |
| ٢ | استيراد من `src/content/` مباشرة في مكوّن | بيكسر طبقة العزل | نادِ دالة من `src/lib/queries.ts` |
| ٣ | `ml-` `mr-` `pl-` `pr-` `left-` `right-` `text-left` `text-right` | بتكسر الاتجاه العربي | `ms-` `me-` `ps-` `pe-` `start-` `end-` `text-start` `text-end` |
| ٤ | ألوان خام (`#hex` / `rgb()`) خارج `globals.css` | بتكسر نظام الألوان والوضع الليلي | استخدم رموز `DESIGN.md §2` |
| ٥ | `"use client"` من غير سبب حقيقي | بيبطّئ الموقع | Server Component افتراضيًا |
| ٦ | مكتبة UI جاهزة (MUI/Chakra/shadcn كامل) | بتفرض شكلها وبتكبّر الحجم | ابنِ في `components/ui/` |
| ٧ | حركة على `width`/`height`/`top`/`left` | بتبطّئ الرسم | `transform` و`opacity` بس |
| ٨ | حساب السعر من بيانات المتصفّح | ثغرة أمنية خطيرة | احسبه على السيرفر من `content/courses.ts` |
| ٩ | أي مفتاح سرّي في الكود | تسريب | `.env` + `.env.example` موثّق |
| ١٠ | أرقام أو آراء مُختلقة تُعرض كحقيقية | خداع للزائر | علّم عليها `// 👈 غيّر ده` |
| ١١ | فاصل ` · ` مكتوب كنص جنب رقم | بيتنقل لمكان غلط وبيتقري كأنه صفر عربي «٠» | أعِد صياغة الجملة، أو استخدم `<FactRow>` |
| ١٢ | `.ltr-nums` على جملة كاملة فيها رقم | بتقلب الكلمة العربية للناحية الغلط | حطّها على الرقم لوحده |
| ١٣ | `text-fg` / `text-fg-muted` / `accent.text` جوّه شريط كحلي | بتبقى كحلي على كحلي — نصّ مختفي في الوضع النهاري | كلاس `dark` على السكشن، أو ألوان صريحة (`text-white/70`) — `DESIGN.md §14.7` |
| ١٤ | `inset-inline-start: 50%` + `translateX(-50%)` للتمركز | بيتجمعوا في RTL فالعنصر بيزيح بعرضه كله | `inset-inline: 0` + `margin-inline: auto` — `DESIGN.md §10.6` |

---

## 5. الأعراف (Conventions)

### أسماء الملفات
- المكوّنات: `kebab-case.tsx` → `course-card.tsx`
- اسم المكوّن نفسه: `PascalCase` → `CourseCard`
- الـhooks: `use-*.ts` → `use-scroll-progress.ts`
- الأنواع: كلها في `src/types/index.ts`

### شكل المكوّن
```tsx
/**
 * كارت الكورس — بيظهر في صفحة الكورسات والرئيسية.
 * المقاسات في docs/DESIGN.md §5.3
 */
import { cn } from "@/lib/utils";
import type { Course } from "@/types";

interface CourseCardProps {
  course: Course;
  size?: "sm" | "md" | "featured";
  className?: string;
}

export function CourseCard({ course, size = "md", className }: CourseCardProps) {
  return <article className={cn("...", className)}>...</article>;
}
```

**قواعد:**
- تعليق عربي فوق كل مكوّن يقول: بيعمل إيه + فين مواصفاته
- `interface` للـprops دايمًا، بنفس اسم المكوّن + `Props`
- `className?` في كل مكوّن، ويتدمج بـ`cn()` في الآخر
- `export function` مش `export default` (غير الصفحات — Next بيطلب `default`)

### التعليقات
- **بالعربي** للشرح والمنطق
- **بالإنجليزي** لأسماء المتغيّرات والدوال
- كل ملف مهم يبدأ بتعليق يقول بيعمل إيه

---

## 6. خريطة "عايز أغيّر إيه؟ → روح فين"

| عايز تغيّر | روح لـ |
|-------------|--------|
| الاسم، الإيميل، التليفون، السوشيال | `src/content/site.ts` |
| نصوص الهيرو | `src/content/site.ts` → `hero` |
| القوائم والروابط | `src/content/navigation.ts` |
| الكورسات، الأسعار، المناهج | `src/content/courses.ts` |
| وصف المستويات التلاتة | `src/content/levels.ts` |
| المشاريع | `src/content/projects.ts` |
| المقالات | `src/content/articles.ts` |
| آراء المتدربين | `src/content/testimonials.ts` |
| الأسئلة الشائعة | `src/content/faq.ts` |
| الباقات | `src/content/pricing.ts` |
| الأرقام (إحصائيات) | `src/content/stats.ts` |
| الألوان والخطوط والمقاسات | `src/app/globals.css` (قسم `@theme`) |
| ترتيب سكاشن الرئيسية | `src/app/page.tsx` |
| الهيدر / الفوتر | `src/components/layout/` |
| تفعيل/تعطيل ميزة | `src/content/site.ts` → `features` |
| نوع الكورس (ميني/عادي/برنامج) | `src/content/courses.ts` → `kind` |
| وعد الميني كورس ونموذج تسليمه | `src/content/courses.ts` → `promise` و`delivery` |
| إعدادات SEO العامة | `src/lib/seo.ts` |
| بوابة الدفع | `src/lib/payments/` |

---

## 7. لو الشغل وقف — إزاي تكمّل

1. افتح `docs/PLAN.md §12` (خطة التنفيذ بالخطوات)
2. شوف آخر مرحلة معلّم عليها ✅
3. ابدأ من المرحلة اللي بعدها بالظبط
4. خلّص **كل** بنود المرحلة قبل ما تعدّي للّي بعدها
5. بعد كل مرحلة: `npm run typecheck && npm run build`
6. علّم على المرحلة إنها خلصت في `PLAN.md`
7. اعمل commit برسالة واضحة

**لو لقيت تعارض أو حاجة ناقصة في التوثيق:**
اكتب اللي عملته ووضّح افتراضك في رسالة الـcommit — **ما تقفش الشغل**.

---

## 8. قائمة مراجعة قبل أي commit

- [ ] `npm run typecheck` نضيف
- [ ] `npm run build` نضيف
- [ ] `grep -rE "\b(ml|mr|pl|pr)-[0-9]|text-(left|right)|\b(left|right)-[0-9]" src/` → صفر
- [ ] مفيش نص ظاهر للمستخدم مكتوب جوّه مكوّن
- [ ] الوضع الليلي والنهاري مراجعين
- [ ] **فحص التباين** — مفيش نصّ تحت 4.5:1 (أو 3:1 للعناوين) في الوضعين
      (الطريقة والنتائج في `docs/DESIGN.md §11.1`)
- [ ] جرّبت على عرض 320px — مفيش تمرير أفقي
- [ ] كل زر أيقوني له `aria-label`
- [ ] التوثيق في `docs/` متحدّث لو غيّرت حاجة أساسية

---

## 9. الفرع والـGit

- **فرع التطوير:** `claude/exciting-archimedes-f1blxw`
- ممنوع الدفع لأي فرع تاني من غير إذن صريح
- رسائل الـcommit بالإنجليزي، سطر أول ≤ ٧٢ حرف، وبعدين تفاصيل بالعربي لو محتاج

---

## 10. تذكير أخير

> **الموقع ده واجهة شخص حقيقي.** أي رقم أو رأي أو ادّعاء غير حقيقي بيضرّه، مش
> بيساعده. كل بيانات العيّنة في `src/content/` معلّم عليها `// 👈 غيّر ده` —
> خلّي التعليق ده موجود لحد ما صاحب الموقع يحطّ بياناته الحقيقية.
